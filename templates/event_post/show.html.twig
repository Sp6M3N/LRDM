{% extends 'base.html.twig' %}

{% block title %}EventPost{% endblock %}

{% block body %}
    <h1>EventPost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_post.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ event_post.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_event_post_index') }}">back to list</a>

    <a href="{{ path('app_event_post_edit', {'id': event_post.id}) }}">edit</a>

    {{ include('event_post/_delete_form.html.twig') }}
{% endblock %}
